import { NationalCodePipe } from './national-code.pipe';

describe('NationalCodePipe', () => {
  it('create an instance', () => {
    const pipe = new NationalCodePipe();
    expect(pipe).toBeTruthy();
  });
});
